import { StyleSheet } from 'react-native';

export const bannerMediumSliderStyles = StyleSheet.create({
  container: { width: '100%' },
  containerItem: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
  },
});
